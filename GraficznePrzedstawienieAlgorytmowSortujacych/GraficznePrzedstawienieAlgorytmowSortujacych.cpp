#include "engine.h"

/* #include <SFML/Graphics.hpp>

using namespace sf;

int randomh = 0, part = 1;
int itera = 0; //aktualna iteracja w tablicy
int major = 0, cont = 0, lims = 799;

struct data {
    int height;
    int posx;
}line[800]; */

int main()
{
    srand(static_cast<unsigned>(time(0)));

    Engine engine;

    return 0;
}
